class Solution:
    def returnToBoundaryCount(self, nums: List[int]) -> int:
        pos=0
        count=0
        for i in nums:
            if i>0:
                pos+=i
                if pos==0:
                    count+=1
            elif i<0:
                pos+=i
                if pos==0:
                    count+=1
        return count
