class Solution:
    def maxDepth(self, s: str) -> int:
        count=0
        lon=0
        for i in s:
            if i=='(':
                count+=1
                if count>lon:
                    lon=count
            elif i==')':
                count-=1
        return lon
